# QuickJS vendor directory

Place the QuickJS sources in `thirds/quickjs/quickjs` (quickjs.c, quickjs.h, libregexp.c, libunicode.c, libbf.c, cutils.c, etc.).

If you prefer not to vendor, configure with `-DTRECH_FETCH_DEPS=ON` and CMake will fetch QuickJS.

# nlohmann/json vendor directory

If you want an offline build, place the nlohmann/json repository here so that
`thirds/json/CMakeLists.txt` is available.

Otherwise configure with `-DTRECH_FETCH_DEPS=ON` to fetch it automatically.
